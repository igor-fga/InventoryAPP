<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/activity_margin"
    tools:context=".EditorActivity">

    <!-- Overview category -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Label -->
        <TextView
            android:text="@string/category_overview"
            style="@style/CategoryStyle" />

        <!-- Input fields -->
        <LinearLayout
            android:layout_height="wrap_content"
            android:layout_width="0dp"
            android:layout_weight="2"
            android:paddingLeft="4dp"
            android:orientation="vertical">

            <!-- Name field -->
            <EditText
                android:id="@+id/edit_prod_name"
                android:hint="@string/hint_prod_name"
                android:inputType="textCapWords"
                style="@style/EditorFieldStyle" />

            <!-- Amount field -->
            <EditText
                android:id="@+id/edit_prod_amount"
                android:hint="@string/hint_prod_amount"
                android:inputType="textCapWords"
                style="@style/EditorFieldStyle" />

            <!-- Amount field -->
            <EditText
                android:id="@+id/edit_prod_price"
                android:hint="@string/hint_prod_price"
                android:inputType="number"
                style="@style/EditorFieldStyle" />

            <!-- Sold field -->
            <EditText
                android:id="@+id/edit_prod_sold"
                android:hint="@string/hint_prod_sold"
                android:inputType="number"
                style="@style/EditorFieldStyle" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
